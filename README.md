# Splunk Apps Mirror (sanitized)

This repository mirrors Splunk apps from a local Splunk install (minus local/ and common secrets).
- Each folder under ./apps is one Splunk app.
- Use the Jenkins parameter APP_NAME to export a single app.
